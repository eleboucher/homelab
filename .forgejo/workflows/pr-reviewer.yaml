---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: AI PR Review

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
      - labeled

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ forgejo.workflow }}-${{ forgejo.event.pull_request.number || forgejo.ref }}${{ forgejo.event.action == 'labeled' && format('-label-{0}', forgejo.run_id) || '' }}
  cancel-in-progress: true

jobs:
  review:
    name: AI PR Review
    if: ${{ !forgejo.pull_request.draft }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout PR
        uses: https://github.com/actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ forgejo.event.pull_request.head.sha }}

      - name: Install gh CLI
        run: |
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" > /etc/apt/sources.list.d/github-cli.list
          apt-get update
          apt-get install -y gh

      - name: AI PR Review
        uses: https://github.com/misospace/pr-reviewer-action@529f5e897d0f0f6b203ccb74102ecc586bee538a # v2.0.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          platform: forgejo
          forgejo_api_url: https://git.erwanleboucher.dev/
          forgejo_token: ${{ secrets.GITHUB_TOKEN }}
          allowed_source_hosts: github.com,api.github.com,gitlab.com,registry.terraform.io,artifacthub.io,git.erwanleboucher.dev,codeberg.org
          ai_base_url: https://litellm.erwanleboucher.dev/v1
          ai_api_format: anthropic
          ai_model: MiniMax-M3
          ai_api_key: ${{ secrets.LITELLM_API_KEY }}
          ai_max_tokens: "16000"
          ai_primary_retries: "2"
          ai_primary_retry_delay_sec: "5"
          ai_connect_timeout_sec: "10"
          ai_fallback_base_url: https://litellm.erwanleboucher.dev/v1
          ai_fallback_api_format: openai
          ai_fallback_model: neuralwatt-qwen3-6-35b
          ai_fallback_api_key: ${{ secrets.LITELLM_API_KEY }}
          on_model_failure: notice
          tool_mode: native_loop
          tool_max_requests: "4"
          tool_max_rounds: "2"
          tool_loop_wall_clock_sec: "180"
          tool_request_timeout_sec: "15"
          tool_max_response_bytes: "12000"
          tool_planning_timeout_sec: "60"
          tool_planning_max_context_bytes: "30000"
          tool_planning_max_tokens: "8000"
          tool_allowed_gh_api_repos: "eleboucher/homelab"
          tool_enable_for_forks: "false"
          tool_mcp_servers: "tools=https://mcp-internal.erwanleboucher.dev/mcp,konflate=https://konflate.erwanleboucher.dev/mcp"
          tool_mcp_name_prefixes: "github-mcp,talos-mcp,searxng-mcp,ha-mcp,grafana-mcp,victoria-logs-mcp,context7-mcp"
          publish_review_comment: "true"
