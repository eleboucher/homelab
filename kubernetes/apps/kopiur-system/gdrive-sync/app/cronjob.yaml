---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/batch/cronjob_v1.json
apiVersion: batch/v1
kind: CronJob
metadata:
  name: kopiur-sync-ovh
spec:
  schedule: "0 4 * * *"
  suspend: true
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      activeDeadlineSeconds: 72000
      backoffLimit: 5
      template:
        spec:
          restartPolicy: OnFailure
          serviceAccountName: default
          securityContext:
            fsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            # perfectra1n/volsync bundles kopia + rclone; used here purely as a toolbox.
            - name: kopiur-sync
              image: ghcr.io/perfectra1n/volsync:v0.17.11
              imagePullPolicy: IfNotPresent
              command:
                - /bin/sh
                - -c
                - |
                  set -e -o pipefail

                  export KOPIA_CACHE_DIRECTORY="${KOPIA_CACHE_DIR}"
                  export KOPIA_LOG_DIR="${KOPIA_CACHE_DIR}/logs"
                  export KOPIA_CHECK_FOR_UPDATES=false

                  echo "Connecting to source Kopia repository: filesystem:///repository"
                  # Listing concurrency is separate from copy parallelism.
                  kopia repository connect filesystem \
                    --path /repository \
                    --list-parallelism=16 \
                    --password "${KOPIA_PASSWORD}"

                  echo "Syncing to OVH Object Storage: ${RCLONE_REMOTE_PATH}"
                  kopia repository sync-to rclone \
                    --remote-path "${RCLONE_REMOTE_PATH}" \
                    --list-parallelism=16 \
                    --parallel=8 \
                    --delete

                  echo "Disconnecting from Kopia repository"
                  kopia repository disconnect

                  echo "Synchronization to OVH Object Storage completed successfully"
              env:
                - name: KOPIA_CHECK_FOR_UPDATES
                  value: "false"
                - name: KOPIA_CACHE_DIR
                  value: /tmp/cache
                - name: HOME
                  value: /tmp
                - name: RCLONE_REMOTE_PATH
                  value: "ovh:el-homelab-backup/kopiur"
              envFrom:
                - secretRef:
                    name: kopiur-sync-secret
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
                privileged: false
                readOnlyRootFilesystem: true
                runAsNonRoot: true
              volumeMounts:
                - name: repository
                  mountPath: /repository
                  readOnly: true
                - name: tmp
                  mountPath: /tmp
                - name: rclone-config
                  mountPath: /config/rclone
                  readOnly: true
          volumes:
            - name: repository
              nfs:
                server: normandie.internal
                path: /backups/kopiur
            - name: tmp
              emptyDir: {}
            - name: rclone-config
              secret:
                secretName: kopiur-ovh-secret
