---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: photon
spec:
  chartRef:
    kind: OCIRepository
    name: photon
  interval: 30m
  values:
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    controllers:
      photon:
        containers:
          app:
            image:
              repository: ghcr.io/xyphyn/photon
              tag: v2.4.2@sha256:1ed367f0c73e365adbbe47a5ab3c298b5c78728d1d97fee59aac21152145b5b4
            env:
              PUBLIC_INSTANCE_URL: piefed.erwanleboucher.dev
              PUBLIC_INSTANCE_TYPE: piefedalpha
              PUBLIC_SSR_ENABLED: false
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 10m
                memory: 56Mi
              limits:
                memory: 256Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
    route:
      app:
        parentRefs:
          - name: envoy-external
            namespace: network
        hostnames:
          - photon.erwanleboucher.dev
    service:
      app:
        controller: photon
        ports:
          http:
            port: 3000
